import React from "react";
import ScreenStream from "./ScreenStream";

const App: React.FC = () => {
  return (
    <div className="App">
      <ScreenStream />
    </div>
  );
};

export default App;
